import 'package:flutter/material.dart';

class Server extends StatelessWidget {
   @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text("服务商")
      ),
      body: Center(child: Text('服务商'),),
    );
  }
}